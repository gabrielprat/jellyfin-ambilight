# Override file for using pre-built Docker images
# Usage: docker-compose -f docker-compose.yml -f docker-compose.images.yml up
#
# Set these environment variables before running:
#   export DOCKER_REGISTRY=your-registry.io  # e.g., ghcr.io/username or docker.io/username
#   export DOCKER_TAG=latest                 # or any tag like v1.0.0

services:
  ambilight-extractor:
    image: ${DOCKER_REGISTRY}/jellyfin-ambilight-extractor:${DOCKER_TAG:-latest}
    build: null  # Override build with null to use image instead

  ambilight-player:
    image: ${DOCKER_REGISTRY}/jellyfin-ambilight-player:${DOCKER_TAG:-latest}
    build: null  # Override build with null to use image instead



