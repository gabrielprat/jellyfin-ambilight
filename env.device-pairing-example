# Example: Device-WLED Pairing Configuration
# ==========================================
#
# This example shows how to configure device-specific WLED instances
# for multi-room ambilight setups where different devices should control
# different WLED strips.

# Basic Jellyfin Configuration
JELLYFIN_API_KEY=your-api-key-here
JELLYFIN_BASE_URL=https://your-jellyfin.domain.com

# Default WLED (fallback for unmatched devices)
WLED_HOST=wled-default.lan
WLED_UDP_PORT=21324

# Device Matching Strategy
# Choose which field to match against: DeviceName, Client, or DeviceId
DEVICE_MATCH_FIELD=DeviceName

# Device-WLED Mappings
# Format: WLED_DEVICE_<IDENTIFIER>=hostname:port
# The IDENTIFIER will be matched against the chosen DEVICE_MATCH_FIELD

# Example 1: Match by Device Name
WLED_DEVICE_LIVING_ROOM_TV=wled-livingroom.lan:21324
WLED_DEVICE_BEDROOM_TV=wled-bedroom.lan:21324
WLED_DEVICE_OFFICE_MONITOR=wled-office.lan:21324

# Example 2: Match by Client Application
# WLED_DEVICE_JELLYFIN_WEB=wled-computer.lan:21324
# WLED_DEVICE_ANDROID_TV=wled-androidtv.lan:21324
# WLED_DEVICE_ROKU=wled-roku.lan:21324

# How It Works:
# -------------
# 1. When a Jellyfin device starts playing, the daemon gets the session info
# 2. It extracts the configured field (DeviceName, Client, or DeviceId)
# 3. It looks for a matching WLED_DEVICE_* environment variable
# 4. If found, ambilight data is sent to that specific WLED instance
# 5. If not found, it falls back to the default WLED_HOST

# Matching Examples:
# ------------------
# DeviceName: "Living Room TV" → matches WLED_DEVICE_LIVING_ROOM_TV
# DeviceName: "Bedroom Roku" → matches WLED_DEVICE_BEDROOM (partial match)
# Client: "Jellyfin Web" → matches WLED_DEVICE_JELLYFIN_WEB
# Client: "Android TV" → matches WLED_DEVICE_ANDROID_TV

# LED Configuration (same for all devices in this example)
AMBILIGHT_TOP_LED_COUNT=89
AMBILIGHT_BOTTOM_LED_COUNT=89
AMBILIGHT_LEFT_LED_COUNT=49
AMBILIGHT_RIGHT_LED_COUNT=49

# Other settings
FRAMES_PER_SECOND=10
EXTRACTION_PRIORITY=newest_first
LIBRARY_SCAN_INTERVAL=1800
PLAYBACK_MONITOR_INTERVAL=0.5
LOG_LEVEL=INFO
